#ifndef UNIQUE_NUMBERS_H
#define UNIQUE_NUMBERS_H

#include <vector>
using namespace std;

class UniqueNumbersFinder {
public:
    vector<int> findUniqueNumbers(const vector<int>& numbers);
    void runPartA(const vector<int>& numbers);
};

#endif