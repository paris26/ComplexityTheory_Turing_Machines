#ifndef BRUTEFORCE_H
#define BRUTEFORCE_H

#include <vector>

// Function to find the permutation with the longest sequence of unique numbers using brute force
std::vector<int> longestUniqueSequenceBruteForce(std::vector<int> numbers);

#endif // BRUTEFORCE_H
